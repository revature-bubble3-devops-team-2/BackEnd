apiVersion: v1
kind: Service
metadata:
  name: frontend-service
  namespace: team-magma
  labels:
    app: bubble
spec:
  selector:
    app: bubble
  ports:
    - name: http
      port: 4200
      targetPort: 4200
      protocol: TCP
