apiVersion: v1
kind: Service
metadata:
  name: magma-frontend-service
  namespace: team-magma
  labels:
    app: bubble-app
spec:
  selector:
    app: bubble-app
  ports:
    - name: http
      port: 4200
      targetPort: 4200
      protocol: TCP
