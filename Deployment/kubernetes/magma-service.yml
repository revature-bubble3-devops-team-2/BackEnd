apiVersion: v1
kind: Service
metadata:
  name: magma-service
  namespace: team-magma
  labels:
    app: bubble
spec:
  selector:
    app: bubble
  ports:
  - port: 8080
    targetPort: 8080
    protocol: TCP
    name: http