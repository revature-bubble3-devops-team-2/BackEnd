apiVersion: v1
kind: Service
metadata:
  name: magma-backend-service
  namespace: maxie
  labels:
    app: team-magma-app
spec:
  selector:
    app: team-magma-app
  ports:
    - name: http
      port: 8080
      targetPort: 8080
      protocol: TCP
