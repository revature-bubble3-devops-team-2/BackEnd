apiVersion: v1
kind: Service
metadata:
  name: backend-service
  namespace: team-magma
  labels:
    app: bubble
spec:
  selector:
    app: bubble
  ports:
  - port: 5000
    targetPort: 5000
    protocol: TCP
    name: http